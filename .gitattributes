# Batch files must keep CRLF line endings — cmd.exe misparses LF-only files.
# `-text` stores the bytes verbatim so even raw downloads from GitHub keep
# CRLF (eol=crlf would store LF internally and only convert on checkout).
*.bat -text
